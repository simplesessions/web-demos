---
import Layout from '../../layouts/Layout.astro';
---
<Layout title="prefers-reduced-motion">
  <main class="min-h-screen bg-zinc-950 text-zinc-100">
    <div class="max-w-4xl mx-auto px-6 py-16">
      <a href="/" class="text-zinc-400 hover:text-zinc-200 text-sm mb-8 inline-block">← Back to demos</a>
      <h1 class="text-3xl font-semibold mb-2">prefers-reduced-motion</h1>
      <p class="text-zinc-500 mb-12">
        Respects user preference. Enable "Reduce motion" in macOS Accessibility to see the difference.
      </p>

      <div class="grid md:grid-cols-2 gap-6">
        <div class="reduced-demo rounded-xl border border-zinc-800 bg-zinc-900 p-8">
          <div class="w-12 h-12 rounded-lg bg-amber-500 mx-auto"></div>
          <p class="text-zinc-400 text-sm mt-4 text-center">Animates by default, static when reduced</p>
        </div>
        <div class="rounded-xl border border-zinc-800 bg-zinc-900 p-8">
          <div class="w-12 h-12 rounded-lg bg-zinc-600 mx-auto animate-pulse"></div>
          <p class="text-zinc-400 text-sm mt-4 text-center">Always animates (ignores preference)</p>
        </div>
      </div>
    </div>
  </main>
  <style>
    .reduced-demo div {
      animation: bounce 1s ease-in-out infinite;
    }

    @media (prefers-reduced-motion: reduce) {
      .reduced-demo div {
        animation: none;
      }
    }

    @keyframes bounce {
      0%,
      100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-8px);
      }
    }
  </style>
</Layout>
